# Grip Shot — Project Rules

**Canonical agent rules:** `.cursorrules` (Cursor loads this automatically; keep it updated when rules change).

Claude Code and other assistants: use `.cursorrules` as the full rule set.

**Context:** Amazon seller SaaS monorepo — pnpm, Next.js App Router, Firebase, Stripe; image generation lives in `packages/workflow-core`.
